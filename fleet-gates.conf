# agent-fleet runtime feature gates
# 生产环境通过此文件启用渐进式功能（Guardian 启动的 web 进程无法添加环境变量）
AGENT_FLEET_SESSION_REPOSITORIES_ENABLED=1
AGENT_FLEET_ADOPTION_REPOSITORIES_ENABLED=1
AGENT_FLEET_SUPERVISOR_ENABLED=1
AGENT_FLEET_APPEND_USER_TURN_ENABLED=1
AGENT_FLEET_APPLY_LOCAL_PROFILE_ENABLED=1
